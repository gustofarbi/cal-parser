version: "3"

services:
  renderer:
    image: golang
    build: .
    volumes:
      - ./src:/application
#    command: tail -f /dev/null
    command:
      - go
      - run
      - /application/debug/server.go

#  store:
#    image: minio/minio
#    volumes:
#    - storage:/export

volumes:
  storage:
