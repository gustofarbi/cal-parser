apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "calendar-renderer.fullname" . }}-test-connection"
  labels:
    {{- include "calendar-renderer.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "calendar-renderer.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
