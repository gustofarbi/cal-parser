apiVersion: v1
kind: Service
metadata:
  name: {{ include "calendar-renderer.fullname" . }}
  labels:
    {{- include "calendar-renderer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "calendar-renderer.selectorLabels" . | nindent 4 }}
